const myObject = {
    js: 'javascript',
    cpp: 'C++',
    rb: "ruby",
    swift: "swift by apple"
}
// in worink in key (for-in-key) mainly working in obbject 
for(const key in myObject){
    console.log(key,myObject[key]);
}

// map not allow in 
